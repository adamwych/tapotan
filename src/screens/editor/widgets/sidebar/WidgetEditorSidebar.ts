import * as PIXI from 'pixi.js';
import WidgetEditorSidebarItem from './WidgetEditorSidebarItem';

export default class WidgetEditorSidebar extends PIXI.Container {
    constructor() {
        super();

    }

    public addItem(item: WidgetEditorSidebarItem) {
        
    }
}